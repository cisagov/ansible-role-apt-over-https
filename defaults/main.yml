# List of apt source files to modify.  Files that do not exist will be
# ignored.
apt_source_files:
  - /etc/apt/sources.list
  - /etc/apt/sources.list.d/debian.sources

# A boolean indicating whether or not to modify existing apt package
# repositories to use HTTPS.
prerequisites_only: no
